class Solution {
    public int largestAltitude(int[] gain) {
        int max_sofar = 0;
        int max = 0;
        for(int i=0;i<gain.length;i++){
            max_sofar+=gain[i];
            max = Math.max(max_sofar,max);           
        }
        return max;
    }
}
